function [c,ceq] = fun1(x)
    c = [ (32 * 1000 * x(1)^2) / (pi * x(2)^4) - 1000;  % 强度约束
    (1000 * x(1)^3) / (3 * 7.03e5 * (pi * x(2)^4 / 64)) - 0.01;  % 刚度约束
    (4 * 1000) / (pi * x(2)^2) - 750;]; % 剪切力约束

    ceq = [];
